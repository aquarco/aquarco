# ── PostgreSQL ────────────────────────────────────────────────────────────────
POSTGRES_USER=aquarco
POSTGRES_PASSWORD=aquarco
POSTGRES_DB=aquarco

# ── API ───────────────────────────────────────────────────────────────────────
# postgres service is reachable by service name inside Docker network
DATABASE_URL=postgresql://aquarco:aquarco@postgres:5432/aquarco
PORT=4000
NODE_ENV=development

# ── Web UI ────────────────────────────────────────────────────────────────────
# URL used by the browser (not the Docker network) to reach the API
NEXT_PUBLIC_API_URL=http://localhost:4000/graphql
