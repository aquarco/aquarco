[Unit]
Description=Aquarco Claude Auth Helper
Documentation=https://github.com/owner/aquarco
After=network.target

[Service]
Type=simple
User=agent
Group=agent

ExecStartPre=+/bin/bash -c "mkdir -p /home/agent/.claude && chown -R agent:agent /home/agent/.claude && chmod 700 /home/agent/.claude"
ExecStart=/home/agent/aquarco/supervisor/scripts/claude-auth-helper.sh

Restart=on-failure
RestartSec=5

WorkingDirectory=/home/agent/aquarco

Environment=PATH=/usr/local/bin:/usr/bin:/bin:/home/agent/.local/bin:/home/agent/.npm-global/bin

# Resource constraints
MemoryMax=512M
TasksMax=128

# Security hardening
NoNewPrivileges=true
ProtectSystem=full

StandardOutput=journal
StandardError=journal
SyslogIdentifier=aquarco-claude-auth

[Install]
WantedBy=multi-user.target
